# Optional Python Layer

Baseline workflow: **Excel → SQL → Power BI → Decision**.

Use Pandas + NumPy for EDA, ageing distributions, anomaly detection, sector/district concentration analysis and automation when that adds value.

Suggested use here:
- Pandas: project-register profiling and realisation/job-gap analysis.
- NumPy: threshold sensitivity and outlier checks.

Python is optional; SQL and Power BI remain the governed analytical layers.
